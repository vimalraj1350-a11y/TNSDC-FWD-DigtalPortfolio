<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>My Portfolio</title>
  <style>
    /* Reset */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: Arial, sans-serif;
      background: purple; /* 🔹 Changed to purple */
      color: black;
      scroll-behavior: smooth;
    }

    /* Navbar */
    nav {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      background: #5a2d82; /* 🔹 Dark purple navbar */
      padding: 15px 20px;
      display: flex;
      justify-content: center;
      gap: 30px;
      z-index: 1000;
    }

    nav a {
      color: white;
      text-decoration: none;
      font-weight: bold;
      font-size: 18px;
      transition: color 0.3s;
    }

    nav a:hover,
    nav a.active {
      color: yellow;
      text-decoration: underline;
    }

    /* Sections */
    section {
      min-height: 100vh;
      padding: 100px 20px 50px;
      text-align: center;
    }

    h1 {
      font-size: 40px;
      margin-bottom: 20px;
    }

    p {
      font-size: 18px;
      line-height: 1.6;
    }

    /* About */
    .about-pic {
      width: 300px;
      height: 300px;
      border-radius: 50%;
      border: 6px solid black;
      object-fit: cover;
      margin-bottom: 20px;
    }

    /* Projects */
    .projects-container {
      display: flex;
      flex-direction: column;
      gap: 50px;
      margin-top: 40px;
      align-items: center;
    }

    .project-card img {
      width: 80%;
      max-width: 800px;
      height: 500px;
      object-fit: cover;
      border-radius: 15px;
      border: 5px solid black;
      transition: transform 0.3s;
    }

    .project-card img:hover {
      transform: scale(1.05);
    }

    .project-card p {
      margin-top: 10px;
      font-size: 20px;
      font-weight: bold;
    }

    /* Contact */
    form {
      display: flex;
      flex-direction: column;
      width: 350px;
      margin: auto;
      gap: 15px;
    }

    input, textarea {
      padding: 10px;
      border: 2px solid black;
      border-radius: 8px;
      font-size: 16px;
    }

    button {
      padding: 10px;
      background: #5a2d82; /* 🔹 Purple button */
      color: white;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      font-size: 16px;
      transition: background 0.3s;
    }

    button:hover {
      background: #3d1f5c; /* Darker purple */
    }

    /* Fade-in animation */
    .fade-in {
      opacity: 0;
      transform: translateY(30px);
      transition: opacity 0.8s ease-out, transform 0.8s ease-out;
    }

    .fade-in.visible {
      opacity: 1;
      transform: translateY(0);
    }

    /* Responsive */
    @media (max-width: 768px) {
      .project-card img {
        height: 300px;
      }
      nav {
        gap: 15px;
      }
    }
  </style>
</head>
<body>

  <!-- Navbar -->
  <nav>
    <a href="#home" class="active">Home</a>
    <a href="#about">About</a>
    <a href="#projects">Projects</a>
    <a href="#contact">Contact</a>
  </nav>

  <!-- Home -->
  <section id="home" class="fade-in">
    <h1>Welcome to My Portfolio</h1>
    <p>I am vimalraj 💗.</p>
  </section>

  <!-- About -->
  <section id="about" class="fade-in">
    <h1>About Me</h1>
    <img src="vimal.1.jpg" alt="My Photo" class="about-pic">
    <p>Hello! I'm a web developer passionate about building websites.</p>
    <p>I enjoy learning new technologies and solving real-world problems.</p>
  </section>

  <!-- Projects -->
  <section id="projects" class="fade-in">
    <h1>My Projects</h1>
    <div class="projects-container">
      <div class="project-card">
        <img src="vimal.2.jpg" alt="Project 1">
        <p>Project 1 - Website Design</p>
      </div>
      <div class="project-card">
        <img src="project2.jpg" alt="Project 2">
        <p>Project 2 - Mobile App</p>
      </div>
      <div class="project-card">
        <img src="project3.jpg" alt="Project 3">
        <p>Project 3 - Portfolio UI</p>
      </div>
    </div>
  </section>

  <!-- Contact -->
  <section id="contact" class="fade-in">
    <h1>Contact Me</h1>
    <form>
      <input type="text" placeholder="Your Name" required>
      <input type="email" placeholder="Your Email" required>
      <textarea rows="5" placeholder="Your Message" required></textarea>
      <button type="submit">Send</button>
    </form>
  </section>

  <!-- Script for animations & navbar highlight -->
  <script>
    // Fade-in effect
    const faders = document.querySelectorAll('.fade-in');
    const observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.classList.add('visible');
        }
      });
    }, { threshold: 0.2 });

    faders.forEach(fade => observer.observe(fade));

    // Navbar highlight
    const sections = document.querySelectorAll("section");
    const navLinks = document.querySelectorAll("nav a");

    window.addEventListener("scroll", () => {
      let current = "";
      sections.forEach(section => {
        const sectionTop = section.offsetTop - 150;
        if (scrollY >= sectionTop) {
          current = section.getAttribute("id");
        }
      });
      navLinks.forEach(link => {
        link.classList.remove("active");
        if (link.getAttribute("href") === "#" + current) {
          link.classList.add("active");
        }
      });
    });
  </script>

</body>
</html>
